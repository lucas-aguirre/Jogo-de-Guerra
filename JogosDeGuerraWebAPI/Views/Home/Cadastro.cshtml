<div class="container centered">
    <div class="row">
        <div class="col-sm-9 col-md-7 col-lg-5 mx-auto register-form">
            <div class="card card-signin my-5">
                <div class="card-body form-visual">
                    <h5 class="h5-red text-center">Cadastre-se</h5>
                    <form>
                        <div>
                            @ViewBag.Message
                        </div>
                        <div class="form-group">
                            <label class="label-red" for="Nome">Nome</label>
                            <input name="Name" type="text" class="form-control" id="name" placeholder="Seu Nome" required autofocus>
                        </div>
                        <div class="form-group">
                            <label class="label-red" for="email">Endereço de e-mail</label>
                            <input name="email" type="email" class="form-control" id="email" placeholder="Seu e-mail" required>
                        </div>
                        <div class="form-group">
                            <label class="label-red" for="Password">Sua Senha</label>
                            <input name="Password" type="password" class="form-control" id="email-password" placeholder="Sua senha" required>
                        </div>
                        <div>
                            <button onclick="register()" class="btn btn-default btn-outline-success btn-block text-uppercase" type="button">Cadastrar</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@Styles.Render("~/Content/Login.css")

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/jquery-confirm/3.3.0/jquery-confirm.min.css">
@section MeusScripts{
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-confirm/3.3.0/jquery-confirm.min.js"></script>
    <script type="text/javascript">

        function showError(jqXHR) {            
            var response = jqXHR.responseJSON,
                text_error = "";

            console.log(response);

            //if (response.ModelState.model.Password.lenght > 0) {
            //    text_error = response.ModelState.model.Password[0];
            //} 

            if (response) {
                $.alert({
                    title: 'Oops...',
                    content: text_error,
                    theme: 'modern',
                });
            }
        }

        function register() {
            var loginData = {
                Email: $("#email").val(),
                Password: $("#email-password").val(),
               // ConfirmPassword: $("#confirm-password").val(),
            };
            var createData = {
                Email: $("#email").val(),
                Name: $("#name").val(),
            };

            var logData = {
                grant_type: 'password',
                username: $("#email").val(),
                password: $("#email-password").val(),
            };

            // if (!loginData.Email || !loginData.Password || !loginData.ConfirmPassword) {
            if (!createData.Email || !loginData.Password || !createData.Name) {
                $.alert({
                    title: 'Oops...',
                    content: 'Favor preencher todos os campos',
                    theme: 'modern',
                });
                return false;
            }

            $.ajax({
                type: 'POST',
                url: baseUrl + '/api/Account/Register',
                data: loginData
            }).done(function (data) {
                //$.confirm({
                //    title: 'Sucesso',
                //    content: 'Usuário registrado com sucesso',
                //    theme: 'modern',
                //});
                sessionStorage.setItem('accessToken', data.token_type + " " + data.access_token);
                sessionStorage.setItem('emailUsuario', data.userName);
                Register(createData);
                Login(logData);
            }).fail(showError);
        }

        function Register(createData)
        {    
            $.ajax({
                type: 'POST',
                url: baseUrl + '/api/Usuarios',
                data: createData
            }).done(function (data) {
                console.log("aaaa")
            }).fail(showError);
        }

        function Login (logData)
        {
            $.ajax({
                type: 'POST',
                url: baseUrl + '/Token',
                data: logData
            }).done(function (data) {
              
                window.location.href = baseUrl + "/BatalhasMVC/Index";
            }).fail(showError);
        }
    </script>
}
